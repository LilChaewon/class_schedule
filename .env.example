# 복사해서 .env 로 쓰세요: cp .env.example .env
# 로컬에서 그냥 실행만 할 거면 비워둬도 됩니다(에러 리포팅만 꺼짐).

# 앱 포트
PORT=8080

# Supabase (에러 로그 전송용) — 브라우저에 노출되는 anon 키만 여기에.
SUPABASE_URL=
SUPABASE_ANON_KEY=

APP_ENV=local
RELEASE=dev
